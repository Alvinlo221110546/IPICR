name: CI/CD Pipeline

on:
  push:
    branches: [ main ]  # Jalan setiap kali kamu push ke branch main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # ====================
      # Install & Build Backend
      # ====================
      - name: Setup Node.js for Backend
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install Backend Dependencies
        run: cd backend && npm install

      - name: Run Backend Tests
        run: cd backend && npm test || echo "No backend tests"

      - name: Build Backend Docker Image
        run: |
          cd backend
          docker build -t yourusername/backend-app .

      # ====================
      # Install & Build Frontend
      # ====================
      - name: Setup Node.js for Frontend
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Frontend Dependencies
        run: cd frontend && npm install

      - name: Build Frontend
        run: cd frontend && npm run build

      # ====================
      # Deployment (contoh: Vercel / Render / Docker Hub)
      # ====================
      - name: Deploy (placeholder)
        run: echo "🚀 Deployment step here (Render, Vercel, or Docker Hub)"
